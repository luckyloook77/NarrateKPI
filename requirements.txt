# ─────────────────────────────────────────────────────────────────────
#  NarrateKPI — Python Dependencies
# ─────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.111.0,<1.0.0

# ASGI server
uvicorn[standard]>=0.29.0,<1.0.0

# Data validation (v2)
pydantic>=2.7.0,<3.0.0

# HTTP client (for LLM API calls & Resend email dispatch)
httpx>=0.27.0,<1.0.0

# Markdown → HTML conversion (for email-safe output)
markdown2>=2.4.0,<3.0.0

# LLM: NVIDIA API via OpenAI-compatible SDK
openai>=1.40.0,<2.0.0

# Email: Resend Python SDK
resend>=0.8.0,<1.0.0
